# Task ID: 2
# Title: Design/Generate Supabase PostgreSQL Schema
# Status: pending
# Dependencies: 1
# Priority: high
# Description: Translate the existing 177-field SQLite schema into a PostgreSQL schema compatible with Supabase.
# Details:
Analyze the existing SQLite database schema. Convert data types to their PostgreSQL equivalents (e.g., INTEGER to INT or SERIAL, TEXT to VARCHAR or TEXT, BLOB to BYTEA). Define primary keys, foreign keys, and indexes. Generate SQL scripts for creating the 177 fields across necessary tables. Consider using Supabase's schema editor or migrations feature.

# Test Strategy:
Execute schema creation scripts in the Supabase database. Verify that all tables and columns are created correctly with appropriate data types and constraints using a database tool or Supabase dashboard.
